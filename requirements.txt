# ============================================================
# 可选依赖：仅在本地重建回放（docs/demo/run_replay.py）
# 或再生成样例数据（sample_data/generate_sample_data.py）时需要。
# 控制台页面 docs/demo/console.html 零依赖，双击即开。
# 安装方法：pip install -r requirements.txt
# ============================================================

# --- Excel 数据处理 ---
openpyxl>=3.1.0
pandas>=2.0.0
xlsxwriter>=3.0.0              # 现金流量表合并输出（合并现金流量表.py）

# --- PDF 处理 ---
pdfplumber>=0.10.0
pypdf>=3.0.0
PyMuPDF>=1.23.0               # 对应 import fitz

# --- 网页/网络 ---
requests>=2.28.0
selenium>=4.15.0
webdriver-manager>=4.0.0

# --- 其他工具 ---
pyperclip>=1.8.0               # 剪贴板操作
send2trash>=1.8.0              # 安全删除到回收站

# --- OFD 格式转换 ---
easyofd>=0.1.0

# --- 科学计算（被部分脚本依赖） ---
numpy>=1.24.0
